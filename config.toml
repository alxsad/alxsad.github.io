baseURL = 'https://davidovi.ch'
locale = 'en-us'
title = 'Alex Davidovich - CV'
theme = 'simple-cv'

# One-page CV: no posts, tags, categories, RSS feed or sitemap.
disableKinds = ["page", "section", "taxonomy", "term", "RSS", "sitemap"]
